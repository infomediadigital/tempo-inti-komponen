// Mirrors the breakpoints used by the previous Nuxt viewport configuration.
export const breakpoints = {
  xs: 320,
  sm: 640,
  md: 768,
  lg: 1024,
  xl: 1280,
  '2xl': 1536,
} as const

export type Breakpoints = typeof breakpoints
